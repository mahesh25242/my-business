{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/nextjs",
        "config": {
          "installCommand": "npm install && npx prisma generate"
        }
      }
    ]
  }